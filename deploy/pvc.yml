apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: alert-system
  name: bitcoin-conf
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10M
  volumeMode: Filesystem
